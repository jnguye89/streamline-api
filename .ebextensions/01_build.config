container_commands:
  01_npm_ci:
    command: "npm ci --omit=dev"
    owner: webapp       # run as the app user
    leader_only: true   # one instance does the build
  02_build:
    command: "npm run build"
    owner: webapp
    leader_only: true
